---
title: Select
---

import ComponentPage from "../../components/componentPage";

The Select component is used to display important messages to users. It comes with several sub components to structure the content effectively.

## Installation

To add the Select component to your project, use the Shadcn UI CLI:

```bash
npx shadcn@latest add select
```

## Components

<ComponentPage 
  directory="selects" 
  files={["select-01", "select-02", "select-03"]} 
/>

## Github
Github Repo: [Shadwui](https://github.com/sahilkumardev/shadwui)